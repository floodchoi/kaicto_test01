# 로컬 개발: 로컬 Postgres
DATABASE_URL=postgresql://localhost:5432/meeting_minutes

# 프로덕션(Vercel): Supabase 트랜잭션 풀러 (포트 6543)
# Supabase → Project Settings → Database → Connection pooling → Transaction 의 URI를 복사하고
# [YOUR-PASSWORD] 부분만 실제 DB 비밀번호로 교체.
# DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres

# Gemini API 키는 서버 환경변수가 아니라 웹앱 설정(⚙️)에서 사용자가 직접 입력합니다.
# (브라우저 localStorage에 보관, 요청마다 전달)
